# 导演偏好紧凑提示词模板

本文件只定义可复制排版。所有判断与约束以规则模块为准；没有实际内容的可选栏直接省略。

```text
第<X>集
工具：<项目指定工具与版本；无则省略>
时长状态：<暂估，待正式音频锁定／正式音频已锁定>
前文入场状态：<时间、地点、人物、动作、情绪、道具>
本集信息边界：<本集可揭示内容>
后文承接要求：<结束时必须保留的状态>

【镜头风格·严格锁定】
<真人写实默认或项目确认媒介>，<项目地域、时代、题材、美术、色彩、光影和镜头气质>，画幅：<项目确认值／待确认>，<固定、手持、跟拍的使用边界>。默认单镜不超过4秒；画面统一硬切。无背景音乐；不生成旁白；不生成字幕、字卡或需要准确阅读的画面文字。

【参考图职责映射·严格锁定】（使用参考图时保留）
<@ImageN=资产名＋人物外观／场景外观／位置关系职责＋可识别视角>。

【本集场景资产·严格锁定】
<场景资产名、参考依据或集中固定描述、时段、真实光源、初始空间状态、允许变化>。

【本集人物资产·严格锁定】
<人物资产名、参考依据或集中固定描述、时期关系、初始位置、姿态、视线、受光和道具关系>。

【本集人物声音参考·制作参考】（存在人物对白时保留）
<人物资产名=已确认参考音频或工具绑定；不写文字音色>。

【本集道具资产·严格锁定】（有关键道具时保留）
<道具资产名、集中固定描述、初始持有人、持握方向、位置、起止和交接>。

<以上整集集中资产区只显示一次；以下按生成段依次输出>

第<X>集｜生成段<i>/<N>｜总时长<X.X>秒

制作参考（不得提交视频模型）
【镜头一（0.0–X.X秒）】对应“<原文旁白或对白>”
【镜头二（X.X–X.X秒）】对应“<原文旁白或对白>”
<连续镜头共用一句原文时标明覆盖范围，不复制原句>

视频模型提示词（当前段动态部分从此处开始；提交时先附整集全局锁定块）

【本段资产状态】
人物：<仅在有人物出镜或人物画外连续对白时保留；资产名＋动态状态>。
场景：<场景资产名＋当前时段、光线、人物位置、路径和连续性>。
道具：<仅在实际使用道具时保留；资产名＋持有人、持握、位置、交接和起止>。

【空间与承接】
<左右、前后、高低、面向、距离、视线轴>。摄影机：<机位侧别、高度、观察方向、焦距范围和运动状态>。上一段结束快照：<姿态重心、视线、接触、持握、道具、光源、动作阶段和跨切声音；首段写初始状态>。

【通用负向】
<简短高频生成风险>。

【本段针对性负向】（仅在存在额外真实风险时保留）
<只写本段额外风险>。

【镜头一（0.0–X.X秒）】
<景别>·<主体>·<准确地点>·<初始姿态与视线>·<可选少量情绪词并立即接可见微动作或声音变化>·<触发或同期／画外连续对白>·<连续可见变化>·<结束状态>。

摄影机：<机位与角度>，<焦距>，<景深>。<单一运镜的起止与速度；固定机位则写固定>。

光影与道具：<主光来源、方向、人物受光侧、色温>；<道具起止状态>。

音频：<对白声场、呼吸、拟音、必要音效、按需的一个声音焦点；跨切时写来源、离画状态和承接范围>。无背景音乐；不生成旁白。

连续性与剪辑落点：<继承状态>；<本镜主要变化与结果>；<方向、重心、视线、接触、持握、道具、光线和动作阶段锚点>；<明确硬切落点与必要声音承接>。

【镜头二（X.X–X.X秒）】
<按同一结构续写；镜头数量由内容决定>

长镜头报告（存在超过4秒镜头时保留）
镜头<X>：<X.X秒>；对应原文：“<原句>”；使用理由：<为何不能自然拆分>；可拆方案：<机位与自然切点>；确认状态：<待导演确认／已确认保留>。
```
